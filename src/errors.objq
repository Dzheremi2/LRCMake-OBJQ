define errors

import sys
import qtk from sys

cSelectionException(qtk.exceptions.BaseException):; {
    o:INIT:

    override fthrow(messsage: str):; -> nreturn {
        self.log_message(message)
    }
}